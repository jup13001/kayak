import { createSlice } from '@reduxjs/toolkit'

export const airlinesSlice = new createSlice({
    name: 'airlines',
    initialState: {},
    reducers: {
       
      },
})

export default airlinesSlice.reducer