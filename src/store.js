import { configureStore } from '@reduxjs/toolkit'
import { airlinesSlice } from './features/airlines/airlinesSlice.js'

export default configureStore({
  // function that takes in some info from the prev state and action
  // which then returns the new value of that state
  reducer: {
      
  },
})